allowBuilds:
  # Native image pipeline used by next/image optimization in production.
  sharp: true
  # Native resolver used by eslint-plugin-import-x.
  unrs-resolver: true
  # drizzle-kit's bundler; needed for `drizzle-kit generate` to run.
  esbuild: true
